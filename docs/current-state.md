# Estado de la migración incremental

Auditoría inicial del 23 de septiembre de 2026 y evolución documentada hasta el 24 de septiembre. Se conservaron los datos del proyecto Supabase existente `jumsbafjzjnnlxhulvtk`. El modelo original incluye `profiles` (1 fila), `users` (0), `magic_links` (9), `review_snapshots` (7), `competitors` (20), `competitor_relations` (20), `competitor_snapshots` (80), `alerts` (6), `analytics_visits` (14) y `analytics_clicks` (8). Son cantidades de la auditoría inicial; pueden cambiar.

## Fases realizadas

1. **Seguridad de datos:** RLS en las diez tablas y retirada de permisos directos de `anon` y `authenticated`. Las rutas usan la clave de servicio en servidor. Se comprobó 401 con clave anónima y lectura con clave de servicio.
2. **Identidad y acceso:** se eliminó el login de NextAuth que aceptaba cualquier email. Los enlaces de acceso son aleatorios, tienen hash en la base de datos, caducan en quince minutos y se consumen una vez. Las rutas principales resuelven el perfil desde la sesión y comprueban la propiedad. `npm run dev:local` permite abrir el enlace en la interfaz solo en desarrollo con Supabase y app en localhost.
3. **Modelo y consultas:** migración inicial reconstruida desde el esquema real, preferencias de visita y email, índices de histórico, unicidad de enlaces de competidores. El perfil vincula capturas propias; `competitor_relations` vincula un perfil con `competitors`, y `competitor_snapshots` cuelga del competidor. `alerts` pertenece al perfil. `users` sigue sin relación con `profiles` y está vacío.
4. **Intelligence MVP:** panel «Qué ha cambiado», comparación de valoración y volumen, histórico de solo lectura, insights deterministas, gestión de competidores y alertas existentes. Las comparaciones sin dos capturas se muestran como no disponibles.
5. **Desarrollo sin clientes:** página `/demo` con datos y reseñas ficticias; interfaz de análisis de reseñas desacoplada de la fuente. No se insertan esas reseñas en Supabase.
6. **Resumen semanal:** endpoint protegido, preferencia por perfil desactivada inicialmente, flag global desactivado e idempotencia en Resend. `vercel.json` programa una comprobación diaria a las 08:00 UTC; cada perfil recibe como máximo un resumen cada siete días si existe una captura propia reciente o reseñas propias recientes en la muestra. El correo prioriza hallazgos sustentados y pasos concretos sin repetir citas textuales; enlaza al panel para consultar toda la evidencia. El cron está desplegado, pero falta comprobar un envío real con remitente verificado.
7. **Capturas piloto con procedencia:** las capturas heredadas conservan `legacy_google_places`; las introducidas por un propietario se guardan como `manual_owner`, con fecha de observación y una clave única por negocio y día UTC. Las capturas manuales de competidores tienen propietario propio aunque el registro del competidor sea compartido. La primera captura crea una línea base; las comparaciones requieren otra captura cercana del mismo origen. Las migraciones se probaron en Supabase local y se aplicaron al proyecto remoto sin alterar las cifras heredadas.
8. **Captura propia mediante Business Profile:** conexión OAuth vinculada al perfil y al Place ID; token de actualización cifrado, tabla privada con RLS, captura diaria idempotente de valoración y volumen, y origen `google_business_profile` separado de capturas manuales y heredadas. El flujo se ha probado con respuestas simuladas. No hay conexión real todavía: faltan aprobación de Google, credenciales OAuth y una ficha verificada administrada por el usuario. La migración de la tabla se aplicó a Supabase local y remoto; no contiene conexiones.
9. **Textos propios aportados por el propietario:** importador CSV limitado y validado, declaración de derechos, deduplicación, procedencia y eliminación desde la interfaz. La declaración y el origen indicado por el propietario **no están verificados por ReviewFlow**. El panel muestra señales sobre horario, espera y atención solo con al menos dos reseñas de apoyo, incluyendo citas. Se probó localmente con cuatro reseñas ficticias; la tabla remota estaba vacía en esa comprobación. Estas señales describen la muestra, sin atribuir causalidad a los cambios de valoración.
10. **Lectura temporal de reseñas propias:** el CSV acepta una columna final `id` opcional. Un ID estable dentro del origen declarado permite corregir la misma reseña al reimportarla; sin ID, la identidad depende de fecha, estrellas y texto. El propietario puede borrar una reseña concreta o todas. Solo se aceptan fechas publicadas en los últimos 90 días y el cron diario purga los textos que salen de esa ventana. El panel analiza como máximo 200 reseñas recientes, cuenta las fechadas en los últimos siete días y, cuando existe una visita anterior marcada, las fechadas después de ese día. Compara menciones en los últimos 30 días frente a los 30 anteriores: exige al menos tres reseñas en cada periodo, dos menciones recientes y un aumento de 30 puntos porcentuales. Si hay más de 200 reseñas elegibles, no emite hallazgos de tendencia sobre una muestra truncada. El marcado de novedades como vistas es explícito.

## Pendiente antes de operación real

### Valor de las conclusiones

El panel ahora prioriza hallazgos con evidencia y siguiente paso, limita la comparación resumida a tres competidores y deja los cambios brutos en un registro secundario. Si la última captura es antigua, destaca esa falta de cobertura. El histórico comprueba antigüedad, número de días observados y variaciones bruscas de volumen dentro del mismo día que conviene revisar; mantiene las filas originales solo para auditoría. La demo ilustra conclusiones sobre horario, entregas y personas usando exclusivamente textos ficticios, con umbrales de varias menciones y una advertencia sobre causalidad.

Las capturas reales existentes solo contienen valoración y volumen. No hay textos de reseñas en `review_snapshots.data` ni en `competitor_snapshots.data`. Un CSV propio puede señalar menciones sobre horario, esperas o atención **dentro de la muestra aportada**, con origen declarado sin verificar. Para explicar motivos de competidores o automatizar el análisis textual sigue faltando una fuente con derechos claros. Las capturas examinadas son de noviembre y diciembre de 2025; no deben presentarse como actividad reciente.

- Conseguir aprobación de Google Business Profile API y configurar credenciales OAuth de la aplicación, clave de cifrado y ficha de prueba verificada para probar una captura propia real. El análisis propio admite CSV con derechos declarados por el propietario, pero ReviewFlow no verifica el origen; para competidores y texto histórico automático sigue faltando una fuente autorizada con reglas de conservación claras. Places no suministra el corpus completo.
- El cron de capturas propias queda inactivo sin credenciales Google; el cron de email permanece desactivado por configuración.
- El arranque local se verificó: `supabase start` y `supabase db reset` crearon diez tablas con RLS, sin grants de `anon`/`authenticated`. Las dos migraciones posteriores se aplicaron también en local. Se probaron el acceso, el alta de dos perfiles, las capturas, su duplicación diaria y el aislamiento entre propietarios.
- Adaptar las rutas heredadas de captura a una fuente autorizada; ahora están desactivadas por defecto. Se eliminaron las dos rutas duplicadas de alertas que no utilizaba la interfaz. Queda la ruta manual `/api/alerts/save`, también sin llamadas desde la interfaz.
- Revisar el modelo de alias de competidores: ahora el nombre es global al registro del Place ID y se comparte entre perfiles.
- El proyecto ya está desplegado en Producción. Falta validar el envío real de email con un remitente verificado y preparar un entorno Preview aislado; las compilaciones automáticas de Preview carecen hoy de variables de Supabase.
- La extensión `public.http` sigue en el esquema `public` y aparece en el asesor de seguridad de Supabase.

## Próximas fases pequeñas y verificables

1. **Validar operación con una fuente propia documentada:** probar importación, corrección mediante ID estable, borrado individual y total, tendencia de dos periodos y purga tras 90 días con un conjunto de prueba. Confirmar en el proyecto desplegado que el panel conserva el origen declarado y que marcar una visita no oculta los cambios antes de leerlos.
2. **Captura propia real:** obtener aprobación de Google Business Profile API, añadir credenciales OAuth de la aplicación y conectar una ficha verificada. Verificar una captura real, su procedencia y la comparación con una segunda captura en otra fecha.
3. **Fuente competitiva permitida:** elegir una fuente con derechos claros para textos y conservación. Probar con un único competidor y una pregunta concreta de valor, por ejemplo si aumentan las menciones positivas sobre entregas, antes de ampliarlo.
4. **Resumen semanal real:** verificar un remitente en Resend, activar el flag para una cuenta piloto y comprobar que el correo cita solo hallazgos con evidencia y no se duplica.

## Riesgos de datos heredados

`competitors.profile_id` era nulo en las 20 filas auditadas; la pertenencia está en `competitor_relations`. `competitor_snapshots.place_id` era nulo en las 80 filas y se identifica mediante `competitor_id`. La tabla `alerts` tiene `payload`, no `message`. El contenido JSON de las capturas heredadas debe revisarse frente a las condiciones de la fuente antes de una producción multiusuario.
